#!/usr/bin/env bash

source "$(dirname $0)/vars"

verify_app_name "$1"
storage_for_app "$APP"

while read VOLUME
do
    echo " -v" "$STORAGE_VOLUMES$STORAGE_LINE":"$VOLUME"
done < $STORAGE_PATHS

cat
